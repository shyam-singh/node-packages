/**
 * Copyright (c) 2019, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
import { AddressableService, ContainerContext, RequestOutput, RequestParams, RequestService } from '@webruntime/api';
export declare class ComponentService extends AddressableService implements RequestService {
    mappings: {
        [key: string]: any;
    };
    private projectDir;
    private moduleDir?;
    private customModuleDirs;
    private modules;
    constructor({ projectDir, moduleDir, compilerConfig: { lwcOptions } }: {
        projectDir: any;
        moduleDir: any;
        compilerConfig: {
            lwcOptions: any;
        };
    });
    initialize(): Promise<void>;
    request(specifier: string, pivots: RequestParams, { compilerConfig }: ContainerContext): Promise<RequestOutput>;
    toSpecifier(url: string): string;
}
//# sourceMappingURL=component-service.d.ts.map