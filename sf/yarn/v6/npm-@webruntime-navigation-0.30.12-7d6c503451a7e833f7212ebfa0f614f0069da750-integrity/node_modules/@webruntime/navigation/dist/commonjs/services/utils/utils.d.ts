/**
 * Copyright (c) 2019, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
import { RequestOutput } from '@webruntime/api';
export declare const WEBRUNTIME_NAMESPACE = "webruntime";
export declare const NAVIGATION_NAMESPACE = "navigation";
/**
 * Create a request service response with the given data.
 *
 * @param specifier - Bare specifier that was requested
 * @param compilerInput - Object to pass to the compiler
 */
export declare function createRequestOutput(specifier: string, compilerInput: object): Promise<RequestOutput>;
/**
 * Create an error diagnostic for the given message.
 *
 * @param specifier - Bare specifier that failed somehow
 * @param message - Error message
 */
export declare function createErrorDiagnostic(specifier: string, message: string): RequestOutput;
/**
 * Convert a bare specifier or camel case string to an HTML-friendly DOM/property name.
 * @example "sample/app" => "sample-app"
 * @example "sample/listItem" => "sample-list-item"
 *
 * @param str
 */
export declare function toElementName(str: string): string;
/**
 * Create a class name out of a namespace and an id
 * @example ('webruntimeView', 'default') => 'WebruntimeViewDefault'
 *
 * @param namespace
 * @param id
 */
export declare function toClassName(namespace: string, id: string): string;
//# sourceMappingURL=utils.d.ts.map