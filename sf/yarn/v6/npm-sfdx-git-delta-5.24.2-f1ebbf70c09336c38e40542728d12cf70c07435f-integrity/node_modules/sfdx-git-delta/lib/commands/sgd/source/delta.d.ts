import { flags, SfdxCommand } from '@salesforce/command';
import { AnyJson } from '@salesforce/ts-types';
export default class SourceDeltaGenerate extends SfdxCommand {
    static description: string;
    protected static flagsConfig: {
        to: flags.Discriminated<flags.String>;
        from: flags.Discriminated<flags.String>;
        repo: flags.Discriminated<flags.String>;
        ignore: flags.Discriminated<flags.String>;
        'ignore-destructive': flags.Discriminated<flags.String>;
        source: flags.Discriminated<flags.String>;
        'ignore-whitespace': flags.Discriminated<flags.Boolean<boolean>>;
        output: flags.Discriminated<flags.String>;
        'api-version': flags.Discriminated<flags.Number>;
        'generate-delta': flags.Discriminated<flags.Boolean<boolean>>;
        include: flags.Discriminated<flags.String>;
        'include-destructive': flags.Discriminated<flags.String>;
    };
    run(): Promise<AnyJson>;
}
