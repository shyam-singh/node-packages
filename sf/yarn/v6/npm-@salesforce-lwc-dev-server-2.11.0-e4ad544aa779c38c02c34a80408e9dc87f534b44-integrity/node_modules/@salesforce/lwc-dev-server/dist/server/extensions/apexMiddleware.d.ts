/// <reference types="node" />
import { RequestPromiseAPI } from 'request-promise-native';
import { ResourceLoader, FetchOptions } from 'jsdom';
import { AppExtensionConfig } from '@webruntime/api';
export interface ConnectionParams {
    instanceUrl: string;
    accessToken: string;
    timeout?: number;
}
export declare class ApexResourceLoader extends ResourceLoader {
    private readonly orgRequest;
    private readonly instanceUrl;
    constructor(orgRequest: RequestPromiseAPI, instanceUrl: string);
    fetch(url: string, options: FetchOptions): Promise<Buffer> | null;
}
export declare function apexMiddleware(connectionParams: ConnectionParams): {
    extendApp: ({ app }: AppExtensionConfig) => void;
};
//# sourceMappingURL=apexMiddleware.d.ts.map