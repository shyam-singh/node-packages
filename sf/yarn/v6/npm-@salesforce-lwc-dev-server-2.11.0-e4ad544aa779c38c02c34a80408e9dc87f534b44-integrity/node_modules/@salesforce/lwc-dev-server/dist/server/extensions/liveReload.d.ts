import { AppExtensionConfig } from '@webruntime/api';
import WebruntimeConfig from 'server/config/WebruntimeConfig';
import Project from 'common/Project';
export declare function liveReload(metadataPath: string, project: Project, config: WebruntimeConfig): {
    extendApp: ({ app }: AppExtensionConfig) => Promise<void>;
    close: () => Promise<void>;
};
export declare function getFilesToWatch(metadataPath: string, project: Project): string[];
//# sourceMappingURL=liveReload.d.ts.map