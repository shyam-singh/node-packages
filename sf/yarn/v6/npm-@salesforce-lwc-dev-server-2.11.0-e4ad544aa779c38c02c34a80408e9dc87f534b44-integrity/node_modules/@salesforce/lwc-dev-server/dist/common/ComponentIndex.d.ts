import Project from './Project';
export default class ComponentIndex {
    private project;
    constructor(project: Project);
    getProjectMetadata(): ProjectMetadata;
    getModules(): PackageComponent[];
    private findModulesIn;
    private findSubdirectories;
    private isJSComponent;
    private isUIComponent;
    private nameFromFile;
    private splitPath;
    private findProjectMetadata;
}
export interface ProjectMetadata {
    projectName: string;
    packages: PackageMetadata[];
}
export interface PackageMetadata {
    key: string;
    isDefault: boolean;
    packageName: string;
    components: PackageComponent[];
}
export interface PackageComponent {
    htmlName: string;
    jsName: string;
    name: string;
    namespace: string;
    url: string;
    path: string;
}
//# sourceMappingURL=ComponentIndex.d.ts.map