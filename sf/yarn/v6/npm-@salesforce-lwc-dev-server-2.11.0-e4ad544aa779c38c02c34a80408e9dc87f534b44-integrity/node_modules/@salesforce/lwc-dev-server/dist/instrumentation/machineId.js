"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const crypto_1 = __importDefault(require("crypto"));
const os_1 = __importDefault(require("os"));
const invalidMacAddresses = new Set([
    '00:00:00:00:00:00',
    'ff:ff:ff:ff:ff:ff',
    'ac:de:48:00:11:22'
]);
function getMacAddress() {
    try {
        const networkInterfaces = os_1.default.networkInterfaces();
        const macAddresses = Object.keys(networkInterfaces)
            .reduce((macAddresses, networkInterfaceName) => {
            const networkInterfaceInfo = networkInterfaces[networkInterfaceName] || [];
            macAddresses = macAddresses.concat(networkInterfaceInfo.map(info => {
                return info.mac;
            }));
            return macAddresses;
        }, [])
            .filter(macAddress => {
            return !invalidMacAddresses.has(macAddress);
        });
        if (macAddresses.length > 0) {
            const macAddress = macAddresses[0];
            return macAddress;
        }
    }
    catch (error) {
        return undefined;
    }
}
exports.getMacAddress = getMacAddress;
function getMachineId() {
    const macAddress = getMacAddress();
    return macAddress
        ? crypto_1.default
            .createHash('md5')
            .update(macAddress)
            .digest('hex')
        : '';
}
exports.getMachineId = getMachineId;
//# sourceMappingURL=machineId.js.map