import { Plugin } from 'rollup';
import { RequestParams } from '../options';
import { ServiceDefinition } from '../service-definition';
/**
 * A service which generates components during the Compilation phase of a Container.build() request.
 */
export interface CompileService extends ServiceDefinition {
    getPlugin(params: RequestParams): Plugin;
}
export declare function supportsCompilation(service: ServiceDefinition): service is CompileService;
//# sourceMappingURL=compile-service.d.ts.map