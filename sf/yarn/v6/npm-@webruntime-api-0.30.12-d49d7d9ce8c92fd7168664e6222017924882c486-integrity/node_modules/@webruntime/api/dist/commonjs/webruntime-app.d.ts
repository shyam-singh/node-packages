/**
 * Copyright (c) 2019, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
import { ApplicationConfig } from './config';
import { PageDefinition } from './webruntime-page';
/**
 * The Base LWR Page Class which the default LWR page and custom pages extend.
 *
 * Create a custom Page class extended from WebruntimePage:
 * @example
 *```js
 * const {WebruntimeAppDefinition} = require('@webruntime/api');
 * const SamplePage = require('src/server/sample-page.js');
 *
 * class SampleApp extends WebruntimeAppDefinition {
 *  get pages() {
 *      return [{
 *          route: '/*',
 *          page: SamplePage
 *      }]
 *  }
 * }
 * ```
 */
export declare abstract class WebruntimeAppDefinition {
    readonly config: ApplicationConfig;
    get pages(): PageDefinition[];
    constructor(config: ApplicationConfig);
    get defaultComponent(): string;
    get defaultTemplate(): string;
}
export declare type WebruntimeAppDefinitionCtor = {
    new (config: ApplicationConfig): WebruntimeAppDefinition;
};
//# sourceMappingURL=webruntime-app.d.ts.map