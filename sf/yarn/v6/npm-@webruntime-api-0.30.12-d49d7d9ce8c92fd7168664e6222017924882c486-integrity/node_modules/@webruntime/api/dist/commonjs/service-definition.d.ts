/** @hidden */
/**
 * Copyright (c) 2019, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
import { PublicConfig } from './config';
import { ImportMapObject } from './options';
import { RequestService } from './services/request-service';
import { CompileService } from './services/compile-service';
/**
 * The definition of a container service that provides one or more addressable components.
 */
export interface ServiceDefinition {
    initialize(): Promise<void> | void;
    shutdown(): Promise<void> | void;
}
export interface UriServiceDefinition extends ServiceDefinition {
    uri: string | string[];
    mappings: ImportMapObject<string>;
    toSpecifier(url: string): string;
    parseUrl(url: string): string[] | null;
}
export declare type ServiceDefinitionCtor = {
    new (serviceConfig: PublicConfig): RequestService | CompileService;
};
/**
 * A service is addressable via:
 *      URI => request service
 *      specifier => compile service
 *
 * @param service
 */
export declare function isAddressableService(service: Object): service is ServiceDefinition;
//# sourceMappingURL=service-definition.d.ts.map