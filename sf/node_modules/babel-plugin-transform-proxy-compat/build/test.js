const { transform } = require('@babel/core');
const transformPlugin = require('../build/index');

const res = transform(`
Object.keys({});
Object.values({});

`, {
    babelrc: false,
    plugins: [[transformPlugin, {}]],
});

console.log(res.code)