#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@salesforce/lwc-dev-server/bin/run" "$@"
  ret=$?
else 
  node  "$basedir/../@salesforce/lwc-dev-server/bin/run" "$@"
  ret=$?
fi
exit $ret
