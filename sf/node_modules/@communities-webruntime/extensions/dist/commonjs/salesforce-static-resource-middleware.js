"use strict";
/** @hidden */
/**
 * Copyright (c) 2019, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs = __importStar(require("fs"));
const path_to_regexp_1 = require("path-to-regexp");
const constants_1 = require("./constants");
const regexp = path_to_regexp_1.pathToRegexp(`/${constants_1.ASSETS_DIR}/:versionKey/(.*)`);
function rewriteUrl(url) {
    const parts = regexp.exec(url) || [];
    return parts[2] && `/${constants_1.ASSETS_DIR}/${parts[2]}`;
}
/**
 * Handle Salesforce static resource URLs imported using @salesforce/resourceUrl/<resourceName>
 * and @salesforce/contentAssetUrl/<assetFileName>.
 */
function salesforceStaticResourcesMiddleware({ outputDir }) {
    return (req, res, next) => {
        const assetUrl = rewriteUrl(req.url);
        const assetFile = assetUrl && `${outputDir}${assetUrl.split('?')[0]}`;
        if (assetFile && fs.existsSync(assetFile)) {
            req.url = assetUrl;
        }
        next();
    };
}
exports.salesforceStaticResourcesMiddleware = salesforceStaticResourcesMiddleware;
//# sourceMappingURL=salesforce-static-resource-middleware.js.map