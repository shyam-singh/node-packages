/**
 * Copyright (c) 2020, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
import { executeGlobalController } from 'webruntime/aura';
import { apiCall } from 'webruntime/apiCall';

jest.mock('@app/basePath', () => '', { virtual: true });
jest.mock('@app/csrfToken', () => '', { virtual: true });
jest.mock('webruntime/apiCall');
jest.mock('forceChatterApi/util', () => ({}), { virtual: true });

describe('webruntime/aura', () => {
    describe('executeGlobalController', () => {
        it('is apiCall', () => {
            return expect(executeGlobalController).toBe(apiCall);
        });
    });
});
