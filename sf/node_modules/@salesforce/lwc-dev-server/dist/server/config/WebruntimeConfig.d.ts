import Project from '../../common/Project';
import { Config, ApplicationConfig, CompilerConfig, BundleConfigEntry, CompileMode, ServiceDefinitionCtor, ContainerAppExtension } from '@webruntime/api';
import { Plugin } from 'rollup';
export default class WebruntimeConfig implements Config {
    serverDir: string;
    projectDir: string;
    buildDir: string;
    moduleDir?: string;
    server: any;
    app?: ApplicationConfig;
    services: ServiceDefinitionCtor[];
    bundle: BundleConfigEntry[];
    preloadModules: string[];
    externals?: string[];
    compilerConfig: CompilerConfig;
    defaultMode: CompileMode;
    defaultLocale: string;
    constructor(project: Project);
    addMiddleware(middleware: ContainerAppExtension[]): void;
    addRoutes(routes: ContainerAppExtension[]): void;
    addModules(modules: string[]): void;
    addPlugins(plugins: Plugin[]): void;
    addServices(services: ServiceDefinitionCtor[]): void;
}
//# sourceMappingURL=WebruntimeConfig.d.ts.map