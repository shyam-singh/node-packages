import Project from '../common/Project';
import { Connection } from '@salesforce/core';
export default class LocalDevServer {
    private server;
    private config;
    private project;
    private liveReload?;
    private readonly sessionNonce;
    private readonly vendorVersion;
    private reporter;
    constructor(project: Project, connection?: Connection);
    shutdown(): Promise<void>;
    start(): Promise<void>;
    get serverPort(): number | undefined;
    private getSupportedCoreVersions;
}
//# sourceMappingURL=LocalDevServer.d.ts.map