export = ResourceHandler;
declare class ResourceHandler extends StandardHandler {
    handleDeletion(): Promise<void>;
    _buildMatchingFiles(elementName: any): string[];
    _buildElementMap(srcPath: any): Promise<void>;
}
import StandardHandler = require("./standardHandler");
