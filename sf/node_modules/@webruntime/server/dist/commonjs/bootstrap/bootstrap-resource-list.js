"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const api_1 = require("@webruntime/api");
const bootstrap_resource_generator_1 = require("./bootstrap-resource-generator");
async function getBootstrapResourceList(container, config, page, pivotOverrides) {
    const pivots = {
        locale: config.defaultLocale,
        mode: config.defaultMode,
    };
    if (pivotOverrides) {
        Object.assign(pivots, pivotOverrides);
    }
    // Validate that the request param "mode" is supported
    const compileMode = pivots.mode;
    if (!Object.values(api_1.CompileMode).includes(compileMode)) {
        pivots.mode = config.defaultMode;
    }
    return bootstrap_resource_generator_1.generate(api_1.BOOTSTRAP_SPECIFIER.fullyQualified, pivots, container, config, page);
}
exports.getBootstrapResourceList = getBootstrapResourceList;
//# sourceMappingURL=bootstrap-resource-list.js.map